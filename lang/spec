Types:
    int
    float
    bool
    unit - ()
    string
    vector & hashmap
    gadt

Syntax:
    
    if a:
        x
    else:
        y

    let x = if a then 1 else 2
    
    let add = (int x, (int -> bool -> float) y) -> float:
        y x false

    let add = (x, y) => do:
        x + y

    let add = (x, y) => x + y
    
    let add = (int x, float y) -> bool:
        return x

    struct Foo:
        int age
        string name
        

    
